

### ✅ Readme: 
Create a README file to answer the following question-

 - What is JSX, and why is it used?
 ANS: JSX is a syntax that lets you write HTML-like code inside JavaScript, making React UI building easier and more readable.


 - What is the difference between State and Props?
 ANS: State is internal data that a component manages itself, while Props are external data passed to a component from its parent.


 - What is the useState hook, and how does it work?
 ANS: useState is a React hook that lets you add state to functional components by returning a state value and a function to update it.


 - How can you share state between components in React?
 ANS: You can share state between components by lifting the state up to their common parent and passing it down as props, or by using global state tools like Context API, Redux, or Zustand.


 - How is event handling done in React?
 ANS: Event handling in React is done by attaching events to elements using camelCase event names (like onClick) and passing a function to handle the event.

---

